# HTTP port the API listens on
PORT=3000

# Path to the SQLite database file.
# Use ":memory:" for an ephemeral in-memory database (used by tests).
DATABASE_PATH=syncflow.sqlite

# Application environment. When set to "production", TypeORM schema
# synchronization is disabled (use migrations instead).
NODE_ENV=development
